kind: Subgraph
version: v2
definition:
  name: app
  generator:
    rootPath: .
    namingConvention: none
  includePaths:
    - metadata
  envMapping:
    APP_LOKI_READ_URL:
      fromEnv: APP_LOKI_READ_URL
    APP_LOKI_WRITE_URL:
      fromEnv: APP_LOKI_WRITE_URL
  connectors: []
